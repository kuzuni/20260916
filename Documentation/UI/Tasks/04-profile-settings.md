# Additional profile/settings dialogs — references 25–30

Archive the six user images from Desktop/메트로배니아 outside Assets with original Korean filenames in the manifest. Inspect full-resolution originals. Implement the new child dialogs in the social module, preserving the existing parent instance/tab and independent modal dim/close/input blocking.

Use runtime uGUI, real nickname input, gender/portrait selection, 20 separate portrait sprites with reusable empty frames, an exclusive 11-language list and scrollable local blocked list. Nickname confirmation spends 200 demo rubies exactly once when valid. Account actions and translation remain unconnected demonstrations. Preserve generation prompts; do not paste screenshots into the UI.

Required evidence: hosted Unity compile and interaction tests, all six nested captures at 9:16 and 9:19 with safe insets, parent input blocked while the child is open, top-only close, restored parent tab/scroll and exact state changes. Added routes increase the catalog to 30 and capture inventory to 66 (six main cases plus two per route). Coordinator integrates incremental progress into main and fast-forwards the original local checkout without running local Unity or changing user material.
